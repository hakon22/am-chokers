/** Один закрытый период самовывоза по календарным дням (границы включительно), даты в формате YYYY-MM-DD */
export interface PickupBlockedDateRangeInterface {
  startDate: string;
  endDate: string;
}
