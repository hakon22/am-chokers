import type { ThemeConfig } from 'antd';

// Placeholder theme config — replace with real v3 tokens
export const v3Theme: ThemeConfig = {
  token: {
    colorPrimary: '#6c63ff',
    fontFamily: 'Oswald, sans-serif',
    borderRadius: 6,
  },
};
